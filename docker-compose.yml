networks:
  dev-net:
    driver: bridge

services:
  api-java:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8082:8082
    volumes:
      - .:/app
    networks:
      - dev-net
    restart: always
